apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "easy-chart.fullname" . }}-test-connection"
  labels:
    {{- include "easy-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "easy-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

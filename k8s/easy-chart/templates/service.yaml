apiVersion: v1
kind: Service
metadata:
  name: {{ include "easy-chart.fullname" . }}
  labels:
    {{- include "easy-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easy-chart.selectorLabels" . | nindent 4 }}
